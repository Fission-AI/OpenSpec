Phase 17 manual-test is complete. I rebuilt the CLI with `pnpm run build`, then ran three fresh-context smoke scenarios against copied fixtures: staggered repo archive, `soft-done` to explicit workspace `hard-done`, and standalone repo-local archive outside workspace mode. All three behaved as required: the staggered case stayed top-level `in-progress`, the completion case showed `soft-done` before `archive --workspace` and `hard-done` after it, and the standalone repo-local archive still wrote to `openspec/changes/archive/` and removed the active change directory.

I updated [MANUAL_TEST.md](/Users/tabishbidiwale/fission/repos/openspec/notes/workspace-poc/phase-17-test-workspace-archive/MANUAL_TEST.md) to record the exact scenarios, results, fixes applied, and residual risk status. No product code changes were needed. The Phase 17 checklist in [ROADMAP.md](/Users/tabishbidiwale/fission/repos/openspec/ROADMAP.md:578) was already fully checked and remains accurate, so no checkbox edit was necessary.