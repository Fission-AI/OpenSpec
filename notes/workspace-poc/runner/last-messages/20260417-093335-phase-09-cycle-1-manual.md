Phase 09 manual-test is complete.

I rebuilt with `pnpm run build` and reran the fresh copied-fixture smoke for:
- `workspace open --json`
- `new change shared-refresh --targets app,api` + `workspace open --change shared-refresh --agent claude`
- `new change shared-broken --targets app,docs` + `workspace open --change shared-broken`
- `workspace open --agent codex`

All scenarios passed. Planning-only open exposed no repo roots, change-scoped open attached only `app` and `api`, the stale-target failure named `docs` and pointed to `workspace doctor`, and the Claude path stayed session-prep only with no `.claude` file or repo-local materialization written.

I updated [MANUAL_TEST.md](/Users/tabishbidiwale/fission/repos/openspec/notes/workspace-poc/phase-09-test-workspace-open/MANUAL_TEST.md) to record the fresh run and clarify that the dirty fixture’s stale alias points to `repos/docs-missing`. No product fixes were required, and `ROADMAP.md` did not need changes.