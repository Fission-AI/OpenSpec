Phase 23 manual-test is complete. I rebuilt the CLI with `pnpm run build`, then ran a fresh copied-fixture smoke in a temp sibling `workspace`/`repos` layout against the built CLI. The scenarios covered: adding `docs` to an existing change, confirming the new target appears in `workspace open --change ... --json` and `status --change ... --json`, materializing it with `apply`, verifying removal is then blocked by the authority-handoff guardrail, and separately removing an unmaterialized `docs` target and confirming `open`, `status`, and `apply` all respect the reduced target set.

Observed behavior matched the Phase 23 contract: metadata and `targets/docs/` were created on add, removed cleanly on unmaterialized removal, `workspace open` and workspace-aware `status` reflected the adjusted target set immediately, `apply` materialized the added target, and later removal of that materialized target failed with the explicit repo-local authority error. No product fixes were needed. I updated [MANUAL_TEST.md](/Users/tabishbidiwale/fission/repos/openspec/notes/workspace-poc/phase-23-workspace-target-set-adjustment/MANUAL_TEST.md); the Phase 23 checkboxes in `ROADMAP.md` were already marked complete and remain so.