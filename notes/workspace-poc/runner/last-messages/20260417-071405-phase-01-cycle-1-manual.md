Manual-test pass for Phase 01 is complete. I rebuilt the CLI, then in fresh temporary XDG config/data roots ran `workspace --help`, `workspace create alpha-team-binmanual`, the duplicate-create case, and the invalid-name case through `node bin/openspec.js`. The create flow produced `.openspec/workspace.yaml`, `.openspec/local.yaml`, `changes/`, and `.gitignore` with `/.openspec/local.yaml`, and it did not create any nested `openspec/changes`. No Phase 01 defect surfaced, so no code changes were needed.

I updated [MANUAL_TEST.md](/Users/tabishbidiwale/fission/repos/openspec/notes/workspace-poc/phase-01-workspace-create/MANUAL_TEST.md). [ROADMAP.md](/Users/tabishbidiwale/fission/repos/openspec/ROADMAP.md) already had the Phase 01 checkboxes marked complete on disk, so no checkbox edit was necessary.

Verification: `pnpm run build`; manual smoke via `node bin/openspec.js --no-color workspace ...` in isolated XDG roots.