Phase 16 manual testing is complete for this pass. I rebuilt the CLI with `pnpm run build`, then ran two fresh real-CLI scenarios against copied happy-path workspace fixtures: an early `archive --workspace` attempt that failed as expected with the explicit `in-progress`/`soft-done` error, and the full repo-local-archive-then-workspace-archive flow that stayed `soft-done` until the explicit top-level archive and then moved to `hard-done` while preserving repo-local archive boundaries.

No product defects were found, so no code fixes were needed. I updated [MANUAL_TEST.md](/Users/tabishbidiwale/fission/repos/openspec/notes/workspace-poc/phase-16-workspace-archive/MANUAL_TEST.md) to reflect both scenarios and the observed results. `ROADMAP.md` was left unchanged because the Phase 16 checkboxes were already complete and still matched the verified behavior.