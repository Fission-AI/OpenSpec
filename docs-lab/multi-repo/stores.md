# Stores (beta)

> Plan changes that span repositories: one store, many repos.

<!-- Skeleton: headings only. This page owns the fact that a store moves the planning
home: `openspec/` paths shown across the docs (e.g. reference/skills.md's Creates
cells) are the default repo-local layout, and with a store those artifacts live in the
store repo instead. Reference pages state the default without a caveat and rely on
this page to teach the exception. -->

## What a store is

## When you need one (and when you don't)

## Setup

## The daily flow

## Beta limits
