# Project configuration

> Teach OpenSpec your project in config.yaml: context, rules, and conventions.

<!-- Skeleton: headings only. config.yaml usage only; the field spec is
reference/configuration/config-yaml.md (link, don't restate). -->

## config.yaml at a glance

## Context and rules

## Language and conventions
