# schema-instruction Delta Spec — PR #843 CodeRabbit Review Fixes

## Purpose

Ensure the `spec-driven` schema's instruction artifact fully documents all delta spec features available to AI agents — including scenario-level merge tags (`(MODIFIED)`, `(REMOVED)`) — so that agents producing delta specs are aware of fine-grained merge capabilities.
