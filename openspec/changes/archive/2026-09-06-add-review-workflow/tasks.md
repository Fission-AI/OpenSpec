# Tasks: add-review-workflow

## 1. Template authoring (source of truth)

- [x] 1.1 Create `src/core/templates/workflows/review.ts` exporting `getReviewSkillTemplate()` and `getOpsxReviewCommandTemplate()`, following the `verify-change.ts` module pattern; skill body = the instructions of `docs/openspec-review/SKILL.md` verbatim, with the two self-references authored as `/opsx:review` per design D3, frontmatter fields per design D4, and store-selection guidance matching the sibling templates; command body adapted per the sibling opsx command templates. Verify: `pnpm build` succeeds and a scratch script imports both factories and prints both templates without throwing.
- [x] 1.2 Re-export both factories from `src/core/templates/skill-templates.ts`. Verify: importing the barrel module resolves both names (build + a one-line node check).

## 2. Pipeline registration

- [x] 2.1 Register the workflow in `src/core/shared/skill-generation.ts`: add `{ template: getReviewSkillTemplate(), dirName: 'openspec-review', workflowId: 'review' }` to `getSkillTemplates()` and `{ template: getOpsxReviewCommandTemplate(), id: 'review' }` to `getCommandTemplates()`. Verify: a node check on the built dist shows both lists gained the `review` entry and filtering by `['review']` returns exactly those entries.
- [x] 2.2 Add `'openspec-review'` to `OPENSPEC_SKILL_NAMES` in `src/core/config.ts`. Verify: build succeeds; list length assertions in dependent tests are handled in group 4.
- [x] 2.3 Add `'openspec-review'` to `SKILL_NAMES` and `'review'` to `COMMAND_IDS` in `src/core/shared/tool-detection.ts`. Verify: typecheck passes (`COMMAND_IDS` and `CommandId` flow through consumers).
- [x] 2.4 Add `'review'` to `ALL_WORKFLOWS` in `src/core/profiles.ts` without touching `CORE_WORKFLOWS` (design D2). Verify: `WorkflowId` union includes `review`; a node check confirms `getProfileWorkflows('core')` excludes review.
- [x] 2.5 Add the `'review': 'openspec-review'` mapping to the workflow→skill maps in `src/core/init.ts` and `src/core/profile-sync-drift.ts` (both copies, per the in-file keep-in-sync note). Verify: a node check shows both maps agree on the new key.
- [x] 2.6 Add `'review': 'openspec-review'` to `COMMAND_TO_SKILL_NAME` in `src/utils/command-references.ts`. Verify: `transformToSkillReferences('/opsx:review x')` returns `/openspec-review x`; unknown ids are still left untouched.
- [x] 2.7 Add the `review` entry (name "Review", description of the review–fix cycle) to the workflow metadata map in `src/commands/config.ts`. Verify: `pnpm build && node dist/... config list` (or the built CLI's config listing) shows Review among available workflows.

## 3. Distribution parity (generator owns skills/)

- [x] 3.1 Run `pnpm build && pnpm generate:skills` and confirm `git diff skills/` is empty against the committed `skills/openspec-review/SKILL.md` (design Risks, first bullet: the generated bytes are the arbiter; if the diff is non-empty, fix the template authoring in 1.1 until the generated file matches the reference definition's content). Verify: empty `git diff skills/` after regeneration.
- [x] 3.2 Add the two content hashes for `getReviewSkillTemplate` / `getOpsxReviewCommandTemplate` and the `openspec-review` generated-content hash plus factory pair to `test/core/templates/skill-templates-parity.test.ts` (values produced by the test's own hashing routine). Verify: that test file passes.
- [x] 3.3 Turn the skills.sh parity guard green: `pnpm vitest run test/core/templates/skillssh-parity.test.ts` passes both the per-template content check and the exact-directory-set check (it is red at change start).

## 4. Test guardrails in lockstep

- [x] 4.1 Update `test/core/shared/skill-generation.test.ts` and `test/core/shared/tool-detection.test.ts` expected lists to include `openspec-review` / `review`. Verify: both test files pass.
- [x] 4.2 Update the expected installed skill/command sets in `test/core/init.test.ts` and `test/core/update.test.ts` for the review workflow (including the custom-selection and core-exclusion expectations from the specs). Verify: both test files pass.
- [x] 4.3 Update `test/utils/command-references.test.ts` (id→skill mapping row for `review`) and `test/commands/config-profile.test.ts` (workflow enumeration/metadata). Verify: both test files pass.
- [x] 4.4 Run the full verification gate: `pnpm lint`, typecheck, and the complete test suite. Verify: all green.

## 5. Documentation

- [x] 5.1 Add `openspec-review` to the skill list in `docs/supported-tools.md`. Verify: page renders the new row; no other lists changed.
- [x] 5.2 Add the `openspec-review` section to `docs-lab/reference/skills.md`, a Review entry to `docs-lab/reference/glossary.md`, and the `review` row to `docs-lab/customize/profiles.md` (marked not in core). Verify: each page lists review consistently with the CLI metadata (name "Review").

## 6. Cleanup, coordination, and cross-platform checks

- [x] 6.1 Remove `docs/openspec-review/SKILL.md` (design D5; content preserved in git history and the template). Verify: file deleted, no references to it remain (`grep -r "docs/openspec-review" docs/ src/ test/` is empty).
- [x] 6.2 Add a coordination note to `openspec/changes/unify-template-generation-pipeline/tasks.md` requiring the future `WorkflowManifest` to carry the `review` workflow with its skill/command pair (design D7). Verify: the note appears in that change's task list.
- [x] 6.3 Validate the change: `openspec validate --change "add-review-workflow" --json` reports valid.
- [ ] 6.4 DEFERRED — deliberately left open at archiving time, not silently dropped. What it still owes: the Windows CI leg (`os: windows-latest`, label `windows-pwsh` in `.github/workflows/ci.yml`) has not run on the PR head, where only the `CodeRabbit` check reports, so the green-run evidence this task asks for does not exist yet. The change itself adds no platform-specific behavior (generated tree is plain text written via `node:path` + UTF-8), so the confirmation is a CI observation rather than a code gap. Left unchecked on purpose: ticking it without the run would be an unbacked claim, which this workflow forbids. Next action: read the `windows-pwsh` job once a maintainer-approved CI run lands; if it is red, reopen as its own task.
