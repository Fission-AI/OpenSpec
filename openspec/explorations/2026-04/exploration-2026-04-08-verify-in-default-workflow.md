# Exploration: Verify in Default Workflow

**Date**: 2026-04-08
**Status**: In Progress

## Context

EnpalSpec forked OpenSpec and introduced a new default workflow. Currently the `core` profile
installs: `propose`, `explore`, `apply`, `archive`.

Two goals:
1. Add `verify` to the default workflow — specifically as a step between `apply` and `archive`
2. Install the `enpalspec:verify` command (and its skill) by default during `enpalspec init`

## Rounds

_(to be filled during exploration)_

## Insights & Decisions

_(to be filled at wrap-up)_

## Open Questions

_(to be filled at wrap-up)_
