# The changes

One row per change. Each carries `plan: local` in its `.openspec.yaml`, so
status is live — run `openspec list --plan`, don't update this table.

| Change | What it does |
|---|---|
| simplify-skill-installation | Fewer default skills, quicker first run |
| fix-opencode-commands-directory | Files where OpenCode expects them |
| add-global-install-scope | Choose where tools install |
| schema-alias-support | Friendlier schema names, nothing breaks |
