# Goal: smoother setup

New users should reach a first win fast. Today the first run asks too much:
too many skills up front, files landing where tools don't expect them, no say
in where things install, schema names that confuse.

Done looks like: install, init, first change — with nothing to explain.
